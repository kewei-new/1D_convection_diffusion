# dd2d_smooth_mms

First-stage 2D manufactured-solution drift-diffusion case.

Tracked fields:

- carrier density `n`
- electric potential `phi`
- charge proxy average

Legacy candidates are the optimized `DD_model/模拟/V5` and `DD_model/*验阶*` folders in the 2D repository.
